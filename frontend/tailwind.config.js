/** @type {import('tailwindcss').Config} */
// Tailwind v4: configuração via CSS (@theme no index.css).
// Este arquivo pode ser removido — mantido apenas para compatibilidade de ferramentas.
export default {}